{
  "name": "websocket-server-prac-typescript",
  "version": "0.1.0-SNAPSHOT",
  "description": "Practice WebSocket Server written in TypeScript",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "serve": "nodemon dist/main.js",
    "start": "npm run build && npm run watch",
    "watch": "concurrently \"npm run watch-ts\" \"npm run serve\"",
    "watch-ts": "tsc -w"
  },
  "author": "Ryo Ota",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.0.39",
    "@types/node": "^10.9.2",
    "@types/ws": "^6.0.0",
    "concurrently": "^4.0.0",
    "express": "^4.16.2",
    "nodemon": "^1.12.1",
    "typescript": "^3.0.1",
    "ws": "^6.0.0"
  }
}
